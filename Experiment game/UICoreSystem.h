#pragma once

// #include "UIPointerManager.h"
// #include "UIEventDispatcher.h"
// #include "UIFocusManager.h"
// #include "UIDrawSystem.h"
// // #include "UILayoutEngine.h" // opsional, jika kamu pakai auto layout

// class InputManager;
// class IRendererGUI;

class UICoreSystem {
public:
    UICoreSystem();
    ~UICoreSystem();

//     /**
//      * @brief Dipanggil setiap frame untuk memproses input dan interaksi UI.
//      */
//     void update(InputManager* input);

//     /**
//      * @brief Dipanggil untuk menggambar semua elemen UI.
//      */
//     void render(IRendererGUI* renderer);

//     /**
//      * @brief Reset semua pointer/gesture/fokus (misalnya saat pergantian scene).
//      */
//     void reset();

// private:
//     UIPointerManager pointerManager;
//     UIEventDispatcher eventDispatcher;
//     UIFocusManager focusManager;
//     UIDrawSystem drawSystem;
//     // UILayoutEngine layoutEngine; // opsional

//     // Tambahan internal jika perlu referensi global state, root window, dsb.
};