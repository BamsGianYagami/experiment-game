#pragma once
// #include "MouseInputDevice.h"
#include "UIPointer.h"
#include <vector>

class UIPointerManager {
public:
    UIPointerManager();
    ~UIPointerManager();
//     void update(float deltaTime, const std::vector<IInputDevice*>& devices);
//     void resetAll();

//     int getPointerCount() const;
//     const UIPointer& getPointer(int index) const;

// private:
//     std::vector<UIPointer> activePointers;
};